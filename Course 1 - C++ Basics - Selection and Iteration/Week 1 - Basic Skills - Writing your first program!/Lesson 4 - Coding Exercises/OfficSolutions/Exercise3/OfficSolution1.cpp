#include <iostream>
using namespace std;

int main(int argc, char** argv) {
    
    string line1 = (argv[1]);
    string line2 = (argv[2]);
    
    //add code below this line

    /* Because the string variables are already declared and initialized, you simply need to access them. 
     * You need to use << endl to ensure the newlines are printed after each line. */
    cout << line1 << endl;
    cout << line2 << endl;
    
    //add code above this line
    
    return 0;
    
}