#include <iostream>
using namespace std;

int main() {

    // add code below this line

    /* You can type cast one or both of the variables: */
    int seven = 7;
    int two = 2;
    cout << ( (double) seven / (double) two ) << endl;
    /* Note that the << endl command is optional. */
    
    // add code above this line

    return 0;

}