#include <iostream>
using namespace std;

int main() {

    // add code below this line

    /* The variable names can be anything you want. 
     * One needs to contain the string "Hello " and the other "world". 
     * Don’t forget that there also needs to be a space separating the two words. 
     * To print both variables using just one cout << statement, 
     * use the + operator to concatenate the strings. */
    string a = "Hello ";
    string b = "world";
    cout << (a + b) << endl;
    /* Note that the << endl command is optional. 
     * In addition, the () surrounding the variables are also optional. */

    // add code above this line

    return 0;
    
}